---
import Layout from '../layouts/Layout.astro';
import '../styles/pricing.css';

const pricingPlans = [
  {
    name: "Developer",
    price: "$0",
    period: "/mo",
    description: "Perfect for prototypes and early development",
    features: [
      "Ideal for testing (500 user limit)",
      "All core authentication features",
      "Great for proof-of-concepts"
    ],
    cta: "Start Building →",
    popular: false
  },
  {
    name: "Startup",
    price: "$10",
    period: "/mo",
    description: "For launched applications with real users",
    features: [
      "Handles up to 10,000 users",
      "Everything you need for production",
      "Email support included"
    ],
    cta: "Launch Your App →",
    popular: true
  },
  {
    name: "Scale",
    price: "$50",
    period: "/mo",
    description: "For rapidly growing user bases",
    features: [
      "Supports up to 100,000 users",
      "Only $10 per extra 10K users",
      "Priority feature requests"
    ],
    cta: "Scale Effortlessly →",
    popular: false
  }
];
---

<Layout title="PurelyAuth - User-Based Pricing">
  <main class="pricing-container">
    <h1 class="pricing-title">Pay Only for Your Users</h1>
    
    <div class="pricing-grid">
      {pricingPlans.map((plan) => (
        <div class={`pricing-card ${plan.popular ? 'popular' : ''}`}>
          <div class="card-header">
            <h3>{plan.name}</h3>
            {plan.popular && <span class="popular-badge">Most Popular</span>}
          </div>
          
          <div class="card-price">
            <span class="price">{plan.price}</span>
            <span class="period">{plan.period}</span>
          </div>
          
          <p class="card-description">{plan.description}</p>
          
          <ul class="card-features">
            {plan.features.map((feature) => (
              <li>{feature}</li>
            ))}
          </ul>
          
          <a href="/signup" class={`card-button ${plan.popular ? 'primary' : 'outline'}`}>
            {plan.cta}
          </a>
        </div>
      ))}
    </div>

    <div class="enterprise-cta">
      <h2>Large user base?</h2>
      <p>Custom pricing available for applications with over 50,000 users.</p>
      <a href="/contact" class="enterprise-button">Contact Us</a>
    </div>

    <div class="pricing-faq">
      <h3>Common Questions</h3>
      <div class="faq-item">
        <h4>How are users counted?</h4>
        <p>We count each unique user who signs up in your system. Inactive users still count toward your total.</p>
      </div>
      <div class="faq-item">
        <h4>What happens if I exceed my limit?</h4>
        <p>We'll automatically charge the overage rate for additional users. You'll never be cut off.</p>
      </div>
      <div class="faq-item">
        <h4>Can I downgrade later?</h4>
        <p>Yes, you can change plans anytime. If you downgrade, you'll need to stay under the new user limit.</p>
      </div>
    </div>
  </main>
</Layout>
